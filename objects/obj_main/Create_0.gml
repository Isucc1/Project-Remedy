var a = layer_get_all();
for (var i = 0; i < array_length(a); i++;)
{
	if layer_get_element_type(a[i] != layerelementtype_tilemap)	layer_depth(a[i], 0)
}

importantObjects[0] = obj_pause
importantObjects[1] = obj_playerMain